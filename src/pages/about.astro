---
import PageBanner from '../components/PageBanner.astro';
import Services from '../components/Services.astro';
import Layout from '../layout/Layout.astro';

const services = await Astro.glob('../services-markdown/*.md');
---

<Layout>
	<PageBanner title="About Us" />
	<section class="about-area mb-4">
		<div class="wrapper">
			<div class="grid">
				<div class="about-area__content flex">
					<header class="about-area__header">
						<h2>We're knowledgeable about making benefits higher</h2>
					</header>
					<div class="about-area__body">
						<p>
							Demesne far hearted suppose venture excited see had has. Dependent on so
							extremely delivered by. Yet ﻿no jokes worse her why. Bed one supposing
							breakfast day fulfilled off depending questions. Whatever boy her exertion
							his extended. Ecstatic followed handsome drawings entirely mrs one yet
							outweigh. Of acceptance insipidity remarkably is invitation.
						</p>
						<ul>
							<li>Solution for small & large businesses</li>
							<li>Ease you get credit loan amount in your bank account</li>
						</ul>
					</div>
				</div>
				<div class="about-area__images flex">
					<img src="/assets/imgs/img4.jpg" alt="" />
					<img src="/assets/imgs/img5.jpg" alt="" />
				</div>
			</div>
		</div>
	</section>

	<Services
		services={services.filter((service) => service.frontmatter.featured)}
		seeMore={true}
	/>
</Layout>

<style lang="scss">
	.about-area .wrapper {
		--padding: 0 2rem;
	}

	.about-area__content {
		flex-direction: column;
		align-items: flex-start;
		justify-content: center;
	}

	.about-area__images {
		--gap: 2rem;
	}

	.about-area__images img {
		box-shadow: 1px 4px 20px -2px rgb(0 0 0 / 10%);
		padding: 10px;
		background: #ffffff;
		width: 45%;
		margin: 0 auto;
	}

	.about-area__header h2 {
		font-size: 2.5rem;
	}

	.about-area__body p {
		margin-bottom: 1rem;
	}

	.about-area__content ul {
		padding-left: 2rem;
		font-size: 1.25rem;
	}

	.about-area__content li {
		padding-left: 1rem;
	}

	.about-area__content li::marker {
		content: '\f00c';
		display: inline-block;
		font-style: normal;
		font-variant: normal;
		line-height: 1;
		text-rendering: auto;
		font-family: 'Font Awesome 6 Free';
		font-weight: 900;
		text-align: center;
		border-radius: 50%;
		color: #2b6be0;
	}

	@media screen and (min-width: 768px) {
		.about-area .wrapper {
			--padding: 0 3rem;
		}
		.about-area .grid {
			grid-template-columns: 1fr 1fr;
		}

		.about-area__images {
			--gap: 1rem;

			img {
				height: 70%;

				&:nth-child(1) {
					margin-top: 30%;
				}
			}
		}
	}
</style>
